import map from './clothesdlcmap.json';




export const calculate = (gender: 'male' | 'female', type: 'props' | 'components', component: number, drawable: number): [number, string] => {
    return [0, 'INVALID'];
}